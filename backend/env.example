# Database
MONGODB_URI=mongodb+srv://abdulhadiakanni_db_user:4IcebzpHXCDyDrVV@alchemistwebsite.rjnxmqh.mongodb.net/?retryWrites=true&w=majority&appName=alchemistwebsite

# JWT
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRE=7d

# Session (for OAuth)
SESSION_SECRET=your-session-secret-key-here

# Server
PORT=5000
NODE_ENV=development

# CORS
FRONTEND_URL=http://localhost:3000

# Google OAuth
GOOGLE_CLIENT_ID=36554206634-ioaij3h1o4b0u4fitvbtacg38ek6koub.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: OpenAI / LLM configuration
# If you set OPENAI_API_KEY the chat endpoint will attempt to call OpenAI's Chat Completions API.
# Set RAG_ENABLED=false to disable retrieval-augmented generation and fall back to simple echo behavior.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo
RAG_ENABLED=true

# Email configuration (for password reset functionality)
# Enable email functionality (set to True to enable, False to disable)
EMAIL_ENABLED=True

# Gmail SMTP Configuration (recommended)
# Use your Gmail account with an App Password (not your regular password)
# To generate an App Password: Google Account > Security > 2-Step Verification > App Passwords
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password

# Alternative: Custom SMTP Configuration
# If you prefer to use a different email provider or SMTP server
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yourdomain.com
#
# Note: If EMAIL_ENABLED is False or email credentials are not configured,
# the system will use Ethereal Email test accounts for development (emails won't actually be sent)