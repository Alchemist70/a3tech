// SEB utilities removed — provide no-op placeholders to avoid breaking imports.
export const SEB_DOWNLOAD = '';
export function attemptOpenSEB(): void {
  // no-op
}
export function openSebWithUrl(_: string): void {
  // no-op
}
